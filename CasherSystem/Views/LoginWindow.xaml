<Window x:Class="CasherSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="تسجيل الدخول - متجر الملابس" 
        Height="500" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <Window.Resources>
        <Style x:Key="LoginButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <materialDesign:PackIcon Kind="Store" Width="64" Height="64" 
                                   Foreground="{DynamicResource PrimaryHueMidBrush}" 
                                   Margin="0,0,0,20"/>
            <TextBlock Text="متجر الملابس" 
                      FontSize="28" 
                      FontWeight="Bold"
                      Foreground="{DynamicResource PrimaryHueMidBrush}"
                      HorizontalAlignment="Center"/>
            <TextBlock Text="نظام نقاط البيع" 
                      FontSize="16" 
                      Foreground="{DynamicResource MaterialDesignBodyLight}"
                      HorizontalAlignment="Center"
                      Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Login Form -->
        <materialDesign:Card Grid.Row="1" 
                            Margin="40,20" 
                            Padding="30"
                            materialDesign:ElevationAssist.Elevation="Dp4">
            <StackPanel>
                <Image Source="pack://application:,,,/Assets/Logo.png" 
           Width="120" 
           Height="60" 
           Stretch="Uniform"
           HorizontalAlignment="Center"/>
                <TextBlock Text="تسجيل الدخول" 
                          FontSize="24" 
                          FontWeight="Bold"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,30"/>

                <TextBox x:Name="UsernameTextBox"
                        materialDesign:HintAssist.Hint="اسم المستخدم"
                        materialDesign:HintAssist.IsFloating="True"
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="0,0,0,20"
                        Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

                <PasswordBox x:Name="PasswordBox"
                            materialDesign:HintAssist.Hint="كلمة المرور"
                            materialDesign:HintAssist.IsFloating="True"
                            Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                            Margin="0,0,0,20"
                            PasswordChanged="PasswordBox_PasswordChanged"/>

                <TextBlock x:Name="ErrorMessageTextBlock" 
                          Foreground="Red" 
                          HorizontalAlignment="Center"
                          Margin="0,0,0,20"
                          TextWrapping="Wrap"/>

                <Button Margin="0,0,0,10" Content="تسجيل الدخول" 
                       Style="{StaticResource LoginButtonStyle}"
                       Click="LoginButton_Click"
                       HorizontalAlignment="Center"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Footer -->
        <StackPanel Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="بيانات الدخول الافتراضية:" 
                      FontSize="12" 
                      Foreground="{DynamicResource MaterialDesignBodyLight}"
                      HorizontalAlignment="Center"/>
            <TextBlock Text="اسم المستخدم: admin | كلمة المرور: 1234" 
                      FontSize="12" 
                      Foreground="{DynamicResource MaterialDesignBodyLight}"
                      HorizontalAlignment="Center"
                      Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</Window>
