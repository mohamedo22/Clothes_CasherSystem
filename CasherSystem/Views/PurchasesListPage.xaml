<Page x:Class="CasherSystem.Views.PurchasesListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="قائمة المشتريات"
      FlowDirection="RightToLeft"
      Background="{DynamicResource MaterialDesignPaper}">

    <Page.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="إدارة المشتريات" 
                      FontSize="32" 
                      FontWeight="Bold"
                      Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            <TextBlock Text="عرض وإدارة جميع عمليات الشراء المسجلة في النظام" 
                      FontSize="16" 
                      Foreground="{DynamicResource MaterialDesignBodyLight}"
                      Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Main Content -->
        <materialDesign:Card Grid.Row="1" Padding="20">
            <StackPanel>
                <TextBlock Text="قائمة المشتريات" 
                          FontSize="20" 
                          FontWeight="Bold"
                          Margin="0,0,0,20"/>

                <!-- Search and Actions -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search -->
                    <Grid Grid.Column="0" Margin="0,0,10,0" Width="300">
                        <TextBox materialDesign:HintAssist.Hint="ابحث في المشتريات..."
                                Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                                Padding="8,12,40,12"/>

                        <Button HorizontalAlignment="Right"
                                Style="{StaticResource MaterialDesignIconButton}"
                                Click="SearchButton_Click"
                                ToolTip="بحث"
                                Background="Transparent"
                                Width="36"
                                Height="36"
                                Margin="0,0,4,0"
                                VerticalAlignment="Center">
                            <materialDesign:PackIcon Kind="Magnify" 
                                                   Width="20" 
                                                   Height="20"
                                                   Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                        </Button>
                    </Grid>

                    <!-- Action Buttons -->
                    <Button Grid.Column="1"
                           Content="إضافة شراء"
                           Style="{StaticResource ActionButtonStyle}"
                           Click="AddNewPurchaseButton_Click"
                           Margin="0,5,10,5" HorizontalAlignment="Right" Width="120">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Plus" Margin="0,0,5,0" Width="16" Height="16"/>
                                    <TextBlock Text="إضافة شراء" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <Button Grid.Column="2"
                           Content="تحديث"
                           Style="{StaticResource ActionButtonStyle}"
                           Click="RefreshButton_Click">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Refresh" Margin="0,0,5,0" Width="16" Height="16"/>
                                    <TextBlock Text="تحديث" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>
                </Grid>

                <!-- Purchases DataGrid -->
                <DataGrid ItemsSource="{Binding Purchases}"
                         SelectedItem="{Binding SelectedPurchase}"
                         Style="{StaticResource DataGridStyle}"
                         Height="400">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="رقم العملية" Binding="{Binding Id}" Width="80"/>
                        <DataGridTextColumn Header="التاريخ" Binding="{Binding Date, StringFormat=yyyy-MM-dd}" Width="120"/>
                        <DataGridTextColumn Header="المورد" Binding="{Binding Supplier.Name}" Width="150"/>
                        <DataGridTextColumn Header="إجمالي الفاتورة" Binding="{Binding Total, StringFormat=C}" Width="120"/>
                        <DataGridTextColumn Header="المدفوع" Binding="{Binding PaidAmount, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="المتبقي" Binding="{Binding RemainingAmount, StringFormat=C}" Width="100"/>
                        <DataGridTemplateColumn Header="الإجراءات" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="تعديل"
                                               Style="{StaticResource MaterialDesignFlatButton}"
                                               Click="EditPurchaseButton_Click"
                                               Tag="{Binding}"
                                               Margin="2"/>
                                        <Button Content="حذف"
                                               Style="{StaticResource MaterialDesignFlatButton}"
                                               Click="DeletePurchaseButton_Click"
                                               Tag="{Binding}"
                                               Margin="2"
                                               Foreground="Red"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </materialDesign:Card>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource MaterialDesignBody}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding Purchases.Count, StringFormat='عدد المشتريات: {0}'}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Page>